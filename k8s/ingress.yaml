apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shopify-ingress
spec:
  rules:
    - host: shopify.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
